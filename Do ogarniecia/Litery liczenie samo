	int ILE_LITER(string n, char l){
	int d=n.length();
	int ile=0;
	for (int i=0; i < d; i++)
		if (n[i] == l) ile++;
	return ile;

string nap;
char znak;
cout << "napis i litera: ";
cin >> nap;
cin >> znak;
cout << ILE_LITER(nap,znak) << endl;
}
